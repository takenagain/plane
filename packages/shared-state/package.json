{
  "name": "@plane/shared-state",
  "version": "1.3.0",
  "private": true,
  "description": "Shared state shared across multiple apps internally",
  "license": "AGPL-3.0",
  "type": "module",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": "./dist/index.js",
    "./package.json": "./package.json"
  },
  "scripts": {
    "build": "tsdown",
    "dev": "tsdown --watch --no-clean",
    "check:lint": "oxlint --max-warnings=0 .",
    "check:types": "tsc --noEmit",
    "check:format": "oxfmt --check .",
    "fix:lint": "oxlint --fix .",
    "fix:format": "oxfmt .",
    "clean": "rm -rf .turbo && rm -rf .next && rm -rf node_modules && rm -rf dist"
  },
  "dependencies": {
    "@plane/constants": "workspace:^1.3.0",
    "@plane/types": "workspace:^1.3.0",
    "@plane/utils": "workspace:^1.3.0",
    "lodash-es": "catalog:",
    "mobx": "catalog:",
    "mobx-utils": "catalog:",
    "uuid": "catalog:",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@plane/typescript-config": "workspace:^1.3.0",
    "@types/lodash-es": "catalog:",
    "@types/node": "catalog:",
    "tsdown": "catalog:",
    "typescript": "catalog:"
  }
}
